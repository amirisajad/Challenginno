1-چه دستوری در پاورشل برای بررسی وجود یک فایل در مسیر خاص استفاده می‌شود؟
**Test-Path**

2- در پاورشل، برای تبدیل داده‌های JSON به اشیاء قابل استفاده چه دستوری به کار می‌رود؟ 
**ConvertFrom-Json**
3- چه دستور پاورشلی برای ارسال درخواست GET به یک API استفاده می‌شود؟ 
**Invoke-RestMethod**

4- برای افزودن یک ویژگی جدید به یک شیء در پاورشل، از چه دستوری استفاده می‌شود؟ 
**Add-Member**
5-با کدام دستور می‌توان اطلاعات نرم‌افزارهای نصب‌شده روی سیستم را از طریق WMI دریافت کرد؟
**Get-WmiObject**

6-چه حلقه‌ای در پاورشل برای تکرار روی آرایه‌ای از عناصر استفاده می‌شود؟
**foreach**

7- برای دریافت لیستی از آداپتورهای شبکه که دارای IP هستند در پاورشل از چه کلاس WMI استفاده می‌شود؟ 
**Win32_NetworkAdapterConfiguration**
8- برای محاسبه هش MD5 یک فایل در پاورشل از کدام کلاس .NET استفاده می‌شود؟ 
**System.Security.Cryptography.MD5CryptoServiceProvider**

9-توانایی اجرای اسکریپت‌های PowerShell را می‌توان در ماشین محلی فعال یا غیرفعال کرد. کدام دستور برای این منظور استفاده می شود؟
**Set-ExecutionPolicy**

10- کدام نوع شیء در پاورشل برای ساخت پیلودهای ساختارمند ورود (مانند نام کاربری و رمز عبور) مناسب است؟ 
**Hash Table**
